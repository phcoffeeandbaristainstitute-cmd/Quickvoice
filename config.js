// QuickVoice configuration
// Edit these values to set your defaults

const config = {
  currency: {
    code: "PHP",        // ISO 4217 currency code
    symbol: "₱",        // Display symbol
    locale: "en-PH",    // Used for number formatting
  },
  defaultTaxRate: 12,   // Percentage (e.g. 12 = 12%)
  invoicePrefix: "INV", // Prefix for invoice numbers
  receiptPrefix: "RCP", // Prefix for receipt numbers
};

export default config;
